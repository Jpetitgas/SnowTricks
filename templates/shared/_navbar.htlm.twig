<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="/">SymShop</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Basculer la navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor03">
		<ul class="navbar-nav mr-auto">
			{% for c in categoryRepository.findAll() %}
				<li class="nav-item ">
					<a class="nav-link" href="{{path('product_category',{'slug': c.slug})}}">{{c.name}}</a>
				</li>
			{%endfor %}
		</ul>
		<ul class="navbar-nav">
			<li class="nav-item"><a href="{{path('cart_show')}}" class="nav-link"><I class="fas fa-shopping-cart"></I>Panier ({{cartService.total | amout}})</a></li>
			{% if app.user %}
				<li class="nav-item">
				<a href="{{path('purchase_index')}}" class="nav-link">Mes commandes</nav>
				</li>
				<li class="nav-item ">
					<a class="btn  btn-danger" href="{{ path ('security_logout')}}">LOGOUT</a>
				</li>
			{% else %}
				<li class="nav-item ">
					<a class="nav-link" href="#">Inscription</a>
				</li>
				<li class="nav-item ">
					<a class="btn  btn-success" href="{{ path ('security_login')}}">LOGIN</a>
				</li>
			{% endif %}
		</ul>
	</div>
</nav>
